import 'package:flutter/material.dart';

class LogHomeScreen extends StatelessWidget {
  const LogHomeScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Log Screen')),
    );
  }
}
